resources:
- ../base
- postgres.yaml
- volumes.yaml

configMapGenerator:
- name: airflow-configmap
  files:
  - airflow.cfg

secretGenerator:
- name: airflow-secrets
  envs:
  - dev.env
  type: Opaque

patchesStrategicMerge:
- patch.yaml

namePrefix: dev-